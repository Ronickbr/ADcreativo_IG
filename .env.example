# ADCreativo IG - Variáveis de Ambiente
#
# NOTA: A chave do OpenRouter é configurada pelo usuário
# diretamente no app via Configurações (armazenada no localStorage).
#
# Este arquivo existe apenas para documentação.
# Nenhuma variável de ambiente é obrigatória para rodar o projeto.

# APP_URL: URL onde o app está hospedado (usado para meta-referências)
# APP_URL="http://localhost:3000"
